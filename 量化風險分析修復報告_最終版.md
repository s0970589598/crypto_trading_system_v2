# 量化風險分析修復報告（最終版）

**日期**: 2026-02-11  
**問題**: `'total_loss_in_streak'` 和 `trades_df` 參數錯誤  
**狀態**: ✅ 已完全修復

---

## 問題歷程

### 錯誤 1: 缺少 `total_loss_in_streak` 欄位
```
❌ 量化風險分析失敗：'total_loss_in_streak'
```

### 錯誤 2: 不支持 `trades_df` 參數
```
❌ 量化風險分析失敗：__init__() got an unexpected keyword argument 'trades_df'
TypeError: __init__() got an unexpected keyword argument 'trades_df'
```

---

## 根本原因分析

### 問題 1: 空數據返回值不完整
**位置**: `src/analysis/quantitative_risk.py` - `calculate_max_losing_streak()`

**原代碼**:
```python
if self.df is None or len(self.df) == 0:
    return {'max_streak': 0, 'details': []}  # ← 缺少 total_loss_in_streak
```

**影響**: 當沒有交易數據時，返回的字典缺少必要欄位，導致 Web 界面訪問時出錯。

---

### 問題 2: `__init__` 不支持 DataFrame 參數
**位置**: `src/analysis/quantitative_risk.py` - `__init__()`

**原代碼**:
```python
def __init__(self, trades_data_path: str = None):
    # 只支持檔案路徑，不支持直接傳入 DataFrame
```

**影響**: Web 界面無法直接傳入已載入的 DataFrame，必須先保存為檔案再載入。

---

## 完整修復方案

### 修復 1: 完善空數據返回值 ✅

**檔案**: `src/analysis/quantitative_risk.py`

```python
def calculate_max_losing_streak(self) -> Dict:
    """計算最長連續虧損次數（兼容 API）"""
    if self.df is None or len(self.df) == 0:
        return {
            'max_streak': 0, 
            'total_loss_in_streak': 0.0,  # ← 添加此欄位
            'details': []
        }
```

---

### 修復 2: 支持 DataFrame 參數 ✅

**檔案**: `src/analysis/quantitative_risk.py`

#### 2.1 修改 `__init__` 方法

```python
def __init__(self, trades_data_path: str = None, trades_df: pd.DataFrame = None):
    """初始化量化風險分析器
    
    Args:
        trades_data_path: 交易數據路徑（JSON 格式）
        trades_df: 交易數據 DataFrame（可直接傳入，優先於 trades_data_path）
    """
    self.trades_data_path = trades_data_path
    self.df = None
    
    # 初始化所有分析器
    self.kelly_calculator = KellyCriterionCalculator()
    self.tilt_detector = TiltDetector()
    self.emotional_analyzer = EmotionalControlAnalyzer()
    self.skill_scorer = SkillDimensionScorer()
    self.fee_analyzer = FeeAnalyzer()
    self.cooling_checker = CoolingPeriodChecker()
    
    # 如果直接提供了 DataFrame，使用它
    if trades_df is not None:
        self.df = trades_df.copy()
        self._prepare_dataframe()
    # 否則如果提供了數據路徑，載入它
    elif trades_data_path:
        self.load_data(trades_data_path)
```

#### 2.2 重構 `load_data` 方法

```python
def load_data(self, path: str = None):
    """載入交易數據"""
    if path:
        self.trades_data_path = path
    
    if not self.trades_data_path:
        raise ValueError("未提供數據路徑")
    
    try:
        with open(self.trades_data_path, 'r', encoding='utf-8') as f:
            data = json.load(f)
        
        self.df = pd.DataFrame(data)
        self._prepare_dataframe()  # ← 使用共用方法
        
        print(f"✅ 成功載入 {len(self.df)} 筆交易數據")
    except Exception as e:
        print(f"❌ 載入數據失敗：{e}")
        raise
```

#### 2.3 新增 `_prepare_dataframe` 方法

```python
def _prepare_dataframe(self):
    """準備和清洗 DataFrame 數據"""
    if self.df is None or len(self.df) == 0:
        return
    
    # 數據清洗和類型轉換
    if 'pnl' in self.df.columns:
        self.df['pnl'] = pd.to_numeric(self.df['pnl'], errors='coerce')
    
    if 'leverage' in self.df.columns:
        self.df['leverage'] = pd.to_numeric(self.df['leverage'], errors='coerce')
    
    if 'quantity' in self.df.columns:
        self.df['quantity'] = pd.to_numeric(self.df['quantity'], errors='coerce')
    
    if 'fee' in self.df.columns:
        self.df['fee'] = pd.to_numeric(self.df['fee'], errors='coerce')
    
    # 轉換時間
    if 'open_time' in self.df.columns:
        self.df['open_time'] = pd.to_datetime(self.df['open_time'], errors='coerce')
    
    if 'close_time' in self.df.columns:
        self.df['close_time'] = pd.to_datetime(self.df['close_time'], errors='coerce')
    
    # 計算持倉時間（分鐘）
    if 'open_time' in self.df.columns and 'close_time' in self.df.columns:
        self.df['holding_minutes'] = (
            (self.df['close_time'] - self.df['open_time']).dt.total_seconds() / 60
        )
    
    # 判斷盈虧
    if 'pnl' in self.df.columns:
        self.df['is_win'] = self.df['pnl'] > 0
        self.df['is_loss'] = self.df['pnl'] < 0
```

---

### 修復 3: 更新 Web 界面調用 ✅

**檔案**: `pages/review/quality_scoring.py`

```python
# 準備交易數據
trades_for_risk = scores_df.copy()

# 確保必要的欄位存在
if 'pnl' not in trades_for_risk.columns and 'profit_loss' in trades_for_risk.columns:
    trades_for_risk['pnl'] = trades_for_risk['profit_loss']

if 'close_time' not in trades_for_risk.columns and 'exit_time' in trades_for_risk.columns:
    trades_for_risk['close_time'] = trades_for_risk['exit_time']

# 添加 is_loss 欄位
if 'is_loss' not in trades_for_risk.columns:
    trades_for_risk['is_loss'] = trades_for_risk['pnl'] < 0

# 創建風險分析器實例並傳入數據
risk_analyzer = QuantitativeRiskAnalyzer(trades_df=trades_for_risk)
```

---

## 測試驗證

### 1. 單元測試 - 空數據返回值
```bash
python3 -m pytest tests/analysis/test_quantitative_risk.py::TestQuantitativeRiskAnalyzer::test_calculate_max_losing_streak -v
```
**結果**: ✅ PASSED

### 2. 單元測試 - DataFrame 參數
```bash
python3 -m pytest tests/analysis/test_quantitative_risk.py::TestQuantitativeRiskAnalyzer::test_initialization_with_dataframe -v
```
**結果**: ✅ PASSED

### 3. 完整測試套件
```bash
python3 -m pytest tests/analysis/test_quantitative_risk.py -v
```
**結果**: ✅ 25 passed

---

## 新增測試

**檔案**: `tests/analysis/test_quantitative_risk.py`

```python
def test_initialization_with_dataframe(self):
    """測試使用 DataFrame 初始化"""
    # 創建測試數據
    test_data = pd.DataFrame({
        'pnl': [100, -50, 200, -30],
        'close_time': pd.date_range('2024-01-01', periods=4, freq='h'),
        'open_time': pd.date_range('2024-01-01', periods=4, freq='h') - pd.Timedelta(minutes=30)
    })
    
    analyzer = QuantitativeRiskAnalyzer(trades_df=test_data)
    
    assert analyzer.df is not None
    assert len(analyzer.df) == 4
    assert 'is_win' in analyzer.df.columns
    assert 'is_loss' in analyzer.df.columns
```

---

## 使用方式

### 方式 1: 使用檔案路徑（原有方式）
```python
analyzer = QuantitativeRiskAnalyzer(trades_data_path='trades.json')
```

### 方式 2: 直接傳入 DataFrame（新增方式）
```python
import pandas as pd

trades_df = pd.DataFrame({
    'pnl': [100, -50, 200],
    'close_time': [...],
    'open_time': [...]
})

analyzer = QuantitativeRiskAnalyzer(trades_df=trades_df)
```

### 方式 3: 先創建再載入
```python
analyzer = QuantitativeRiskAnalyzer()
analyzer.load_data('trades.json')
```

---

## 改善效果

### 1. 功能完整性
- ✅ 所有返回值包含必要欄位
- ✅ 支持多種數據輸入方式
- ✅ Web 界面可正常使用

### 2. 代碼質量
- ✅ 重構消除重複代碼
- ✅ 添加 `_prepare_dataframe` 共用方法
- ✅ 更好的錯誤處理

### 3. 測試覆蓋
- ✅ 新增 DataFrame 參數測試
- ✅ 所有測試通過
- ✅ 代碼覆蓋率提升

---

## 架構改善

### 之前
```
QuantitativeRiskAnalyzer
├── __init__(trades_data_path)  ← 只支持檔案路徑
├── load_data()                 ← 包含數據清洗邏輯
└── calculate_max_losing_streak()
    └── 空數據返回不完整 ❌
```

### 現在
```
QuantitativeRiskAnalyzer
├── __init__(trades_data_path, trades_df)  ← 支持兩種方式 ✅
├── load_data()                            ← 調用共用方法
├── _prepare_dataframe()                   ← 共用數據清洗 ✅
└── calculate_max_losing_streak()
    └── 空數據返回完整 ✅
```

---

## 影響範圍

### 修復的功能
1. ✅ Web 界面量化風險分析
2. ✅ 最長連損指標顯示
3. ✅ 所有風險指標計算

### 相關檔案
- `src/analysis/quantitative_risk.py` - 核心邏輯（3 處修改）
- `pages/review/quality_scoring.py` - Web 界面（1 處修改）
- `tests/analysis/test_quantitative_risk.py` - 測試（1 處新增）

---

## 總結

### 修復內容
1. ✅ 修復空數據返回值缺少欄位
2. ✅ 添加 DataFrame 參數支持
3. ✅ 重構數據清洗邏輯
4. ✅ 更新 Web 界面調用
5. ✅ 新增單元測試

### 測試結果
- 25 個測試全部通過
- 新增 1 個測試
- 代碼更健壯

### 用戶體驗
- Web 界面恢復正常
- 支持更靈活的數據輸入
- 錯誤處理更完善

---

**修復完成！** ✅

量化風險分析功能現在完全正常，支持多種數據輸入方式。
